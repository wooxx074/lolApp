<div class="container-fluid">
    <div class="row">
        <div class="col-lg-12">
            <div class="col-lg-6">
                <% Player.all.each do |p| %>
                <%= p.name %> <br/>
                <% end %>
            </div>
        </div>
    </div>
</div>